# Rules - Management Commands

No estado atual, o app `rules` nao possui commands proprios.

A manutencao de configuracao ocorre por:
- Admin Django (`SystemConfig`)
- codigo de inicializacao em comandos de outros apps (ex.: `sync_legacy_events`)

# Messaging - Management Commands

No estado atual, o app `messaging` nao possui management commands proprios.

Operacoes relacionadas a templates sao feitas por:
- Admin Django
- migracoes (seed inicial)

Comandos de notificacao efetiva acontecem de forma indireta via tools do modulo `assistant`.
